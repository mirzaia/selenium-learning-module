# Module — Element locators

id, name, CSS, XPath, findElements, and Selenium 4 relative locators. Work through objectives, concepts, exercises, then the checklist. The module targets localhost:8080.
