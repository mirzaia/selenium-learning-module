# Verification

- [ ] `mvn clean compile` succeeds
- [ ] `mvn test` passes
- [ ] Surefire reports exist under `target/surefire-reports`
- [ ] I can explain create → navigate → interact → assert → quit

If the browser cannot start, check Chrome installation and WebDriverManager network/cache access.
