# Exercise: driver management

Compare headless and headed ChromeOptions, set `--window-size=800,600`, and prove a quit session cannot navigate again. Record the observed exception and why it is useful evidence.
