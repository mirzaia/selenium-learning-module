# Objectives

Create a Maven Selenium project, let WebDriverManager resolve ChromeDriver, run a TestNG test, and explain why `quit()` belongs in teardown. Success means `mvn test` passes against the local demo app.
